import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  textError: {
    color: 'red',
    fontSize: 11,
  },
  viewError: {
    marginTop: 4,
  },
  viewErrorHidden: {
    opacity: 0,
  },
  viewErrorVisible: {
    opacity: 1,
  },
  viewMain: {
    // paddingBottom: 8,
  },
});

export default styles;
